[project]
name = "ecosynapse-api"
version = "0.1.0"
description = "EcoSynapse AI backend: evidence-grounded ecological intelligence and intervention planning"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic>=2.8.0",
  "pydantic-settings>=2.4.0",
  "sqlalchemy>=2.0.32",
  "alembic>=1.13.2",
  "python-multipart>=0.0.9",
  "httpx>=0.27.0",
  "networkx>=3.3",
  "numpy>=1.26.0",
  "python-dotenv>=1.0.1",
]

[project.optional-dependencies]
postgres = ["psycopg[binary]>=3.2.0", "pgvector>=0.3.0"]
dev = [
  "pytest>=8.3.0",
  "pytest-asyncio>=0.24.0",
  "ruff>=0.6.0",
  "mypy>=1.11.0",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 110
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"tests/conftest.py" = ["E402"]
"alembic/env.py" = ["E402"]
"alembic/versions/*.py" = ["E501", "I001"]

